#include <math.h>
#include <stdio.h>

int main() {
    int x , y;
    printf("Digite um valor: ");
    scanf("%d",&x);
    
    y = pow (x,2);
    
    printf("O valor ao quadrado é: %d", y );
    
    
    
    return 0;
}
