#include <stdio.h>
#include <math.h>

int main() {
    int x, n;
    printf("Digite o valor de x: ");
    scanf("%d",&x);
    printf("Digite o valor de n: ");
    scanf("%d",&n);
    
    printf("O resultado é: %d", x * 2 << (n-1) );
    
    return 0;
}
